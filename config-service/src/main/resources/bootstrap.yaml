server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/edwarparedes/config-repository
          search-paths: config-data
  application:
    name: config-service